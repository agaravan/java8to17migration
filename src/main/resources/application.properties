server.port=5000
server.address=0.0.0.0
spring.jackson.serialization.indent-output=true
migration.workspace.dir=./workspace
migration.max-concurrent=3
app.version=@project.version@
# Optional: set the full path to your Maven executable if it cannot be auto-detected.
# Useful when the service runs as a Windows Service and Maven is not on the service PATH.
# Example (Windows): migration.maven.executable=F:/Test/Maven-3.9.6/bin/mvn.cmd
# Example (Linux):   migration.maven.executable=/opt/maven/bin/mvn
migration.maven.executable=
